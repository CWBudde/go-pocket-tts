name: Deploy Web App to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25.x

      - name: Build PocketTTS CLIs
        run: |
          ls -la cmd
          go build -o pockettts-tools ./cmd/pockettts-tools

      - name: Download safetensors model assets
        run: |
          set -euo pipefail
          ./pockettts-tools model download \
            --hf-repo kyutai/pocket-tts-without-voice-cloning \
            --out-dir models

      - name: Build Go wasm kernel
        run: |
          mkdir -p web/dist
          GOOS=js GOARCH=wasm go build -trimpath -ldflags="-s -w" -o web/dist/pockettts-kernel.wasm ./cmd/pockettts-wasm

      - name: Copy Go wasm runtime JS
        run: cp "$(go env GOROOT)/lib/wasm/wasm_exec.js" web/dist/wasm_exec.js

      - name: Download voice embeddings
        run: ./pockettts-tools voice download --out-dir voices

      - name: Copy static web assets
        run: cp web/index.html web/main.js web/dist/

      - name: Bundle safetensors model into web app
        run: |
          mkdir -p web/dist/models
          cp models/tts_b6369a24.safetensors web/dist/models/
          cp models/tokenizer.model web/dist/models/

      - name: Bundle voice embeddings into web app
        run: |
          mkdir -p web/dist/voices
          cp voices/*.safetensors voices/manifest.json web/dist/voices/

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
